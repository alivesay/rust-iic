# ld65 config for dhgr40_term.s -- 40-col DHGR true-color terminal
# module.  Loaded at $0900 by boot terminal's ESC L command.

MEMORY {
    DHGR: start = $0900, size = $1700, fill = no, file = %O;
}

SEGMENTS {
    DHGR: load = DHGR, type = ro, align = $100;
}

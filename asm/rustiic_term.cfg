# ld65 config for rustiic_term.s
#
# Produces a fixed 256-byte boot sector at load address $0800.
# The Apple //c boot ROM reads T0/S0 here and JMPs to $0801.

MEMORY {
    BOOT: start = $0800, size = $0100, fill = yes, fillval = $00, file = %O;
}

SEGMENTS {
    BOOT: load = BOOT, type = ro;
}
